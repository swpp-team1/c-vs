// import { fromJS } from 'immutable';
// import { makeSelectProductDetailDomain } from '../selectors';

// const selector = makeSelectProductDetailDomain();

describe('makeSelectProductDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
