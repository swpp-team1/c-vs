// import { fromJS } from 'immutable';
// import { makeSelectRecipeDetailDomain } from '../selectors';

// const selector = makeSelectRecipeDetailDomain();

describe('makeSelectRecipeDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
