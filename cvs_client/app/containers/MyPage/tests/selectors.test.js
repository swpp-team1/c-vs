// import { fromJS } from 'immutable';
// import { makeSelectMyPageDomain } from '../selectors';

// const selector = makeSelectMyPageDomain();

describe('makeSelectMyPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
