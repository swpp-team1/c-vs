// import { fromJS } from 'immutable';
// import { makeSelectProductAllDomain } from '../selectors';

// const selector = makeSelectProductAllDomain();

describe('makeSelectProductAllDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
