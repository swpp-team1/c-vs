// import { fromJS } from 'immutable';
// import { makeSelectRecipeAllDomain } from '../selectors';

// const selector = makeSelectRecipeAllDomain();

describe('makeSelectRecipeAllDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
