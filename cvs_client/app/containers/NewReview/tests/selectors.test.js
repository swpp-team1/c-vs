// import { fromJS } from 'immutable';
// import { makeSelectNewReviewDomain } from '../selectors';

// const selector = makeSelectNewReviewDomain();

describe('makeSelectNewReviewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
