// import { fromJS } from 'immutable';
// import { makeSelectNewRecipeDomain } from '../selectors';

// const selector = makeSelectNewRecipeDomain();

describe('makeSelectNewRecipeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
